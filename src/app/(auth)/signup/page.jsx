'use client'
import SignInPage from '../../sign-in/page'

const SignUp = () => {
  // Use the same SignInPage component but with signup mode
  return <SignInPage defaultMode='signup' />
}

export default SignUp
